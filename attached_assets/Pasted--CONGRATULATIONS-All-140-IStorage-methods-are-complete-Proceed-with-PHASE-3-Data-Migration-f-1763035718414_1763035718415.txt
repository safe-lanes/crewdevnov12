🎉 CONGRATULATIONS! All 140 IStorage methods are complete!

Proceed with PHASE 3: Data Migration from JSON to PostgreSQL

Now we migrate your 1.3 MB of production data from test-data.json to PostgreSQL.

CREATE: server/migrate-json-to-postgres.ts

MIGRATION REQUIREMENTS:

1. **Read Source Data**:
   - Read test-data.json (1.3 MB)
   - Parse JSON structure
   - Log total entities found

2. **Migration Order** (CRITICAL - must respect foreign keys):

Step 1: Independent tables (no foreign keys)
•	users
•	forms
•	availableRanks
•	companyRanks
•	promotionHierarchies
•	dataMasters
•	vessels
•	vesselGroups
Step 2: Tables with single FK dependencies
•	rankGroups (FK → forms)
•	masterDataEntries (FK → dataMasters)
Step 3: Core entities
•	crewMembers (TEXT primary key "A0001")
•	recruitmentCandidates
Step 4: Entities depending on crew
•	appraisalResults (FK → crewMembers, forms)
•	vesselPlanning (FK → crewMembers)
•	restHoursCrewRecords (FK → crewMembers)
•	restHoursDailyRecords (FK → crewMembers)
•	fixedTasks (FK → crewMembers)
•	ncReports (FK → crewMembers)
Step 5: Other entities
•	vesselDrafts, vesselRevisions, vesselRanks
•	rotationPlans, idCounters
•	restHoursVesselRecords
•	variableTasks, drugAlcoholTestRecords
•	vesselViolationComments, officeViolationComments
•	vesselDateLineAdjustments
•	seafarers, revisions (legacy tables)
3. **Use DatabaseStorage Methods** (not raw SQL):
```typescript
   // Example structure
   import { storage } from './storage';
   import fs from 'fs';
   
   async function migrateData() {
     console.log('Starting JSON to PostgreSQL migration...\n');
     
     // Read JSON
     const jsonData = JSON.parse(fs.readFileSync('./test-data.json', 'utf-8'));
     
     // Migrate users
     if (jsonData.users) {
       console.log(`Migrating ${jsonData.users.length} users...`);
       for (const user of jsonData.users) {
         await storage.createUser(user);
       }
       console.log('✅ Users migrated\n');
     }
     
     // ... repeat for all 31 entities in correct order
   }
```

4. **Handle Special Cases**:
   - **ID Counters**: Migrate currentCrewIdCounter, currentFormId, etc. to id_counters table
   - **TEXT Primary Keys**: crew_members.id is TEXT ("A0001"), not auto-increment
   - **JSON Fields**: Drizzle handles automatically, no special parsing needed
   - **Empty Arrays**: Skip if entity array is empty in JSON

5. **Progress Logging**:
```typescript
   console.log(`Migrating ${jsonData.forms.length} forms...`);
   // ... migrate
   console.log(`✅ Forms migrated (${jsonData.forms.length} records)\n`);
```

6. **Error Handling**:
```typescript
   try {
     // migration code
   } catch (error) {
     console.error(`❌ Error migrating ${entity}:`, error);
     throw error; // Stop on first error
   }
```

7. **Verification Report**:
```typescript
   // After migration, query counts
   const crewCount = (await storage.getCrewMembers()).length;
   const formsCount = (await storage.getForms()).length;
   // ... etc
   
   console.log('\n=== MIGRATION VERIFICATION ===');
   console.log(`Crew Members: JSON=${jsonData.crewMembers?.length || 0}, DB=${crewCount}`);
   console.log(`Forms: JSON=${jsonData.forms?.length || 0}, DB=${formsCount}`);
   // ... all entities
```

SCRIPT STRUCTURE:
```typescript
import { storage } from './storage';
import fs from 'fs';

interface JSONData {
  users?: any[];
  forms?: any[];
  crewMembers?: any[];
  // ... all 31 entity types
  currentCrewIdCounter?: string;
  currentFormId?: number;
  // ... all counters
}

async function migrateJsonToPostgres() {
  console.log('🚀 Starting Migration: JSON → PostgreSQL\n');
  console.log('Reading test-data.json...');
  
  const jsonData: JSONData = JSON.parse(
    fs.readFileSync('./test-data.json', 'utf-8')
  );
  
  try {
    // STEP 1: Independent tables
    await migrateUsers(jsonData);
    await migrateForms(jsonData);
    await migrateAvailableRanks(jsonData);
    // ... etc
    
    // STEP 2: Dependent tables
    await migrateRankGroups(jsonData);
    // ... etc
    
    // STEP 3-5: Continue in order
    
    // FINAL: Verify
    await verifyMigration(jsonData);
    
    console.log('\n🎉 MIGRATION COMPLETE!\n');
  } catch (error) {
    console.error('\n❌ MIGRATION FAILED:', error);
    process.exit(1);
  }
}

async function migrateUsers(jsonData: JSONData) {
  if (!jsonData.users || jsonData.users.length === 0) {
    console.log('⏭️  No users to migrate\n');
    return;
  }
  
  console.log(`Migrating ${jsonData.users.length} users...`);
  for (const user of jsonData.users) {
    await storage.createUser(user);
  }
  console.log(`✅ Users migrated (${jsonData.users.length} records)\n`);
}

// ... similar functions for all 31 entities

async function verifyMigration(jsonData: JSONData) {
  console.log('\n📊 VERIFICATION REPORT');
  console.log('='.repeat(50));
  
  const results = [];
  
  // Check each entity
  const dbUsers = await storage.getUser(1);
  results.push({
    entity: 'users',
    json: jsonData.users?.length || 0,
    db: dbUsers ? 1 : 0  // Adjust based on what's queryable
  });
  
  // ... check all entities
  
  // Print table
  results.forEach(r => {
    const status = r.json === r.db ? '✅' : '❌';
    console.log(`${status} ${r.entity}: JSON=${r.json}, DB=${r.db}`);
  });
  
  console.log('='.repeat(50));
}

// Run migration
migrateJsonToPostgres()
  .then(() => process.exit(0))
  .catch(() => process.exit(1));
```

REQUIREMENTS:
1. Create complete migration script
2. Handle all 31 entity types
3. Migrate in correct FK-safe order
4. Use DatabaseStorage methods (not raw SQL)
5. Include progress logging
6. Include error handling
7. Generate verification report comparing JSON vs DB counts
8. Handle ID counters properly

AFTER IMPLEMENTATION:
1. Show me the complete migration script
2. Run: npx ts-node server/migrate-json-to-postgres.ts
3. Show me the complete console output
4. Show me the verification report (entity counts must match)

DELIVERABLE:
- ✅ Complete migration script created
- ✅ Script executes successfully
- ✅ All data migrated to PostgreSQL
- ✅ Verification report shows 100% data transfer
- ✅ No data loss

DO NOT proceed to Phase 4 until migration is verified complete.
