Excellent work on Module 1 testing! 👏

DECISION: Fix the critical sequence issue for ALL tables before continuing.

TASK: Create Comprehensive Sequence Reset Script

The auto-increment sequence issue you found affects ALL tables with serial IDs after migration. This is CRITICAL and must be fixed before we test other modules.

CREATE: server/fix-sequences.ts

This script should reset sequences for ALL 31 tables:
```typescript
import { pool } from './database';

async function fixAllSequences() {
  console.log('🔧 Fixing all auto-increment sequences...\n');
  
  const tables = [
    // Tables with serial ID columns
    'users',
    'forms',
    'rank_groups',
    'available_ranks',
    'promotion_hierarchies',
    'appraisal_results',
    'vessels',
    'vessel_groups',
    'vessel_drafts',
    'vessel_revisions',
    'vessel_ranks',
    'master_data_entries',
    'rotation_plans',
    'drug_alcohol_test_records',
    'rest_hours_vessel_records',
    'rest_hours_crew_records',
    'rest_hours_daily_records',
    'variable_tasks',
    'fixed_tasks',
    'nc_reports',
    'vessel_violation_comments',
    'office_violation_comments',
    'vessel_dateline_adjustments',
    'id_counters'
  ];
  
  for (const table of tables) {
    try {
      const sequenceName = `${table}_id_seq`;
      
      // Reset sequence to max ID in table
      const result = await pool.query(`
        SELECT setval('${sequenceName}', 
          (SELECT COALESCE(MAX(id), 0) FROM ${table})
        );
      `);
      
      const newValue = result.rows[0].setval;
      console.log(`✅ ${table}: sequence set to ${newValue}`);
      
    } catch (error) {
      // Skip tables that don't have sequences (like crew_members with TEXT id)
      console.log(`⏭️  ${table}: no sequence (probably TEXT primary key)`);
    }
  }
  
  console.log('\n✅ All sequences fixed!');
  await pool.end();
}

fixAllSequences().catch(console.error);
```

REQUIREMENTS:
1. Create the script above
2. Run it: `npx tsx server/fix-sequences.ts`
3. Show me the complete output
4. Verify sequences are now correct
5. Test creating a new Available Rank to confirm fix works

After this fix, we can confidently test all other modules without duplicate key errors.

ALSO: Fix the Rank Groups CREATE validation issue you identified.

Then report completion and I'll approve moving to MODULE 2.
