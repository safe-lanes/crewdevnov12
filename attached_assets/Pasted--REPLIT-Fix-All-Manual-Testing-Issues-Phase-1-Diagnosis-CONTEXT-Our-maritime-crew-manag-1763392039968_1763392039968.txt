# REPLIT: Fix All Manual Testing Issues - Phase 1 (Diagnosis)

## CONTEXT
Our maritime crew management system (SAIL ERP) was deployed locally and the testing team found 15 critical bugs across 6 modules. All issues are documented in the attached Word document "Recruitment__1___1_.docx".

You previously helped migrate this application from JSON storage to PostgreSQL database. Now we need to fix all the issues found during manual testing.

## YOUR MISSION - PHASE 1: SYSTEMATIC DIAGNOSIS

**DO NOT START FIXING YET.** First, investigate and diagnose ALL 15 issues, then report your complete findings.

For each issue below, investigate and document:
1. What currently exists (tables, columns, endpoints, routes)
2. What is missing or broken
3. Root cause of the problem
4. Proposed solution

---

## INVESTIGATION CHECKLIST

### PRIORITY 0 - CRITICAL ISSUES

#### ISSUE #1: Company Processing - Data Not Saving
**Symptoms:** Comments not saving, B7 section not saving, Approval data not saving, Attachments not binding after save

**Your Investigation:**
```
[ ] Check database schema:
    - Does 'company_processing' table exist?
    - Run: SELECT column_name, data_type FROM information_schema.columns WHERE table_name='company_processing';
    - List all columns found

[ ] Check API endpoints:
    - Does POST /api/company-processing endpoint exist?
    - Is it registered in routes?
    - Check server/routes/ directory for company-processing files

[ ] Check application logs:
    - Any errors when trying to save company processing data?
    - Check console output and error logs

[ ] Test the endpoint:
    - Try: curl -X POST http://localhost:5000/api/company-processing -H "Content-Type: application/json" -d '{"test":"data"}'
    - What response do you get?

ROOT CAUSE: [Your findings]
MISSING: [What's missing - tables/columns/endpoints]
PROPOSED FIX: [What needs to be created/fixed]
```

#### ISSUE #2: Crew Appraisal - Save Draft Not Working & API Endpoint Failing
**Symptoms:** Form not saving, API endpoint /api/forms/for-rank is failing

**Your Investigation:**
```
[ ] Check database schema:
    - Does 'crew_appraisal' table exist?
    - What columns does it have?

[ ] Check API endpoints:
    - Does GET /api/forms/for-rank/:rankId endpoint exist?
    - Does POST /api/appraisal/draft endpoint exist?
    - Are they registered in routes?

[ ] Test the failing endpoint:
    - Try: curl http://localhost:5000/api/forms/for-rank/[ANY_RANK_ID]
    - What error do you get? (404? 500? Other?)

[ ] Check frontend:
    - Look for the appraisal form component
    - Does it have proper form submission handler?
    - Any console errors when saving draft?

ROOT CAUSE: [Your findings]
MISSING: [What's missing]
PROPOSED FIX: [What needs to be done]
```

#### ISSUE #3: Promotion - Form Not Saving
**Symptoms:** Promotion form not saving data

**Your Investigation:**
```
[ ] Check database schema:
    - Does 'promotions' table exist?
    - What columns does it have?

[ ] Check API endpoints:
    - Does POST /api/promotions endpoint exist?
    - Is it registered?

[ ] Test endpoint:
    - Try posting to /api/promotions
    - What happens?

ROOT CAUSE: [Your findings]
MISSING: [What's missing]
PROPOSED FIX: [What needs to be done]
```

---

### PRIORITY 1 - HIGH PRIORITY ISSUES

#### ISSUE #4: Rotation - Deploy and Reject Buttons Not Working
**Symptoms:** Deploy button not working, Reject button not working

**Your Investigation:**
```
[ ] Check database schema:
    - Does 'vessel_planning' table have status column?
    - What are the valid status values?

[ ] Check API endpoints:
    - Does POST /api/rotation/:id/deploy endpoint exist?
    - Does POST /api/rotation/:id/reject endpoint exist?

[ ] Check frontend:
    - Find the rotation component
    - Check onClick handlers for Deploy and Reject buttons
    - Do they make API calls? To which endpoints?

ROOT CAUSE: [Your findings]
MISSING: [What's missing]
PROPOSED FIX: [What needs to be done]
```

#### ISSUE #5: Crew Pool - Filters Not Working & E1 Period Not Calculated
**Symptoms:** Filters not working, E1. Details of Sea Service (Company) period not calculated

**Your Investigation:**
```
[ ] Check API endpoint:
    - Does GET /api/crew-pool support query parameters?
    - Check the route handler - does it handle filters (search, rank, nationality, status)?

[ ] Check sea service calculation:
    - Does 'sea_service_records' table exist?
    - Is there a calculation for service period (Years, Months, Days)?
    - Check if SQL query includes period calculation

[ ] Check frontend:
    - Does crew pool component send filter parameters?
    - Check network tab - are query params being sent?

ROOT CAUSE: [Your findings]
MISSING: [What's missing]
PROPOSED FIX: [What needs to be done]
```

#### ISSUE #6: Vessel - Filter Not Working
**Symptoms:** Vessel filters not working

**Your Investigation:**
```
[ ] Check API endpoint:
    - Does GET /api/vessels support query parameters?
    - List which parameters are supported (search, vesselType, flag, status, etc.)

[ ] Check the query construction:
    - Does it build WHERE clause from query params?
    - Any errors in the query logic?

[ ] Test:
    - Try: curl "http://localhost:5000/api/vessels?search=test"
    - Does it filter or return all vessels?

ROOT CAUSE: [Your findings]
MISSING: [What's missing]
PROPOSED FIX: [What needs to be done]
```

#### ISSUE #7: Vessel - Download Button Not Working
**Symptoms:** Download button not working

**Your Investigation:**
```
[ ] Check API endpoint:
    - Does GET /api/vessels/export endpoint exist?
    - Does it return CSV format?

[ ] Check frontend:
    - Find the download button component
    - What does onClick handler do?
    - Does it call the export endpoint?

ROOT CAUSE: [Your findings]
MISSING: [What's missing]
PROPOSED FIX: [What needs to be done]
```

#### ISSUE #8: Vessel - Office Matrix Eye Button Not Working
**Symptoms:** Office Matrix Eye button inside vessel not working

**Your Investigation:**
```
[ ] Check API endpoint:
    - Does GET /api/vessels/:id/office-matrix endpoint exist?
    - What data should it return?

[ ] Check frontend:
    - Find the eye button in vessel component
    - What should it do? (Open modal? Navigate?)
    - Check onClick handler

ROOT CAUSE: [Your findings]
MISSING: [What's missing]
PROPOSED FIX: [What needs to be done]
```

---

### PRIORITY 2 - MEDIUM PRIORITY ISSUES

#### ISSUE #9: Recruitment - Hardcoded or Limited Nationality Filter
**Symptoms:** Hardcoded or limited nationality filter but in form there is list of nationality

**Your Investigation:**
```
[ ] Check API endpoint:
    - Does GET /api/nationalities endpoint exist?
    - Does it return a list?

[ ] Check frontend:
    - Find nationality dropdown/input
    - Is it hardcoded array or fetched from API?
    - Is the list limited?

ROOT CAUSE: [Your findings]
MISSING: [What's missing]
PROPOSED FIX: [What needs to be done]
```

#### ISSUE #10: Recruitment - Not Working (General)
**Symptoms:** "Not working" mentioned in document

**Your Investigation:**
```
[ ] Check recruitment module:
    - What specific functionality is broken?
    - Any console errors?
    - Any API call failures?

[ ] Check if related to nationality issue above

ROOT CAUSE: [Your findings]
MISSING: [What's missing]
PROPOSED FIX: [What needs to be done]
```

#### ISSUE #11: Rank - Chief Officer Name & Nationality Input Fields Not Working
**Symptoms:** Rank: Chief Officer Name, Nationality input field not working

**Your Investigation:**
```
[ ] Check frontend:
    - Find the rank form (Chief Officer context)
    - Are Name and Nationality fields disabled or readonly?
    - Check for any JavaScript that might be preventing input

[ ] Check form state:
    - Is form data being updated on onChange?
    - Any validation blocking input?

ROOT CAUSE: [Your findings]
MISSING: [What's missing]
PROPOSED FIX: [What needs to be done]
```

---

## DIAGNOSTIC COMMANDS TO RUN

Run these commands and report results:

```bash
# 1. Check all tables in database
psql $DATABASE_URL -c "\dt"

# 2. Check specific tables mentioned in issues
psql $DATABASE_URL -c "\d company_processing"
psql $DATABASE_URL -c "\d crew_appraisal"
psql $DATABASE_URL -c "\d promotions"
psql $DATABASE_URL -c "\d vessel_planning"
psql $DATABASE_URL -c "\d sea_service_records"

# 3. List all API route files
find server/routes -name "*.ts" -o -name "*.js"

# 4. Check for registered routes in main server file
grep -r "app.use.*api" server/

# 5. Check TypeScript compilation
npx tsc --noEmit

# 6. Check for console errors in logs
tail -100 /var/log/* 2>/dev/null || echo "No log files found"
```

---

## YOUR DELIVERABLE - PHASE 1 REPORT

After completing your investigation, provide a comprehensive report in this format:

```markdown
# PHASE 1 DIAGNOSIS REPORT

## SUMMARY
- Total Issues Investigated: 15
- Database Issues Found: [count]
- API Endpoint Issues Found: [count]
- Frontend Issues Found: [count]
- Other Issues Found: [count]

## DETAILED FINDINGS

### PRIORITY 0 (Critical - 3 issues)

#### Issue #1: Company Processing
- **Current State:** [what exists]
- **Missing:** [what's missing]
- **Root Cause:** [explanation]
- **Fix Required:** [high-level description]
- **Effort:** [small/medium/large]

#### Issue #2: Crew Appraisal
- **Current State:** [what exists]
- **Missing:** [what's missing]
- **Root Cause:** [explanation]
- **Fix Required:** [high-level description]
- **Effort:** [small/medium/large]

[... continue for all 15 issues]

## DATABASE SCHEMA CHANGES NEEDED

List all tables that need to be:
- Created: [list]
- Modified (columns added): [list with columns]
- Unchanged: [list]

## API ENDPOINTS NEEDED

List all endpoints that need to be:
- Created: [list with HTTP method and path]
- Fixed: [list with what's wrong]
- Unchanged: [list]

## FRONTEND FIXES NEEDED

List all frontend issues:
- Component: [name]
- Issue: [description]
- Fix: [what needs to be done]

## IMPLEMENTATION PLAN

Based on findings, here's my recommended fix order:
1. [First fix with justification]
2. [Second fix with justification]
3. [etc.]

## ESTIMATED TIMELINE
- Database fixes: [time estimate]
- API endpoint creation: [time estimate]
- Frontend fixes: [time estimate]
- Testing: [time estimate]
- **Total:** [total estimate]

## READY TO PROCEED?
Once you approve this diagnosis, I'll proceed with Phase 2 (Implementation) in the priority order outlined above.
```

---

## CRITICAL INSTRUCTIONS

1. **DO NOT START IMPLEMENTING YET** - Only diagnose and report
2. **BE THOROUGH** - Check everything, don't assume
3. **DOCUMENT EVIDENCE** - Include actual error messages, query results, file paths
4. **PRIORITIZE ACCURACY** - Better to spend time investigating than fixing wrong things
5. **ASK IF UNCLEAR** - If you can't determine something, ask for clarification

---

## WHAT I NEED FROM YOU

1. Complete investigation of all 15 issues
2. Comprehensive Phase 1 Diagnosis Report
3. Clear implementation plan
4. Wait for my approval before starting Phase 2

Take your time. Accuracy is more important than speed. Start your investigation now and report back with complete findings.