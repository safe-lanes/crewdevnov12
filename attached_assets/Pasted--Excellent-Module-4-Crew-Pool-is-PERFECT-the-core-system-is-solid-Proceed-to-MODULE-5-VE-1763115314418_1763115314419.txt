🎉 Excellent! Module 4 (Crew Pool) is PERFECT - the core system is solid!

Proceed to MODULE 5: VESSELS Testing

Test the Vessels module systematically:

VESSELS MODULE TESTING
======================

Vessels module manages ship information, vessel groups, and vessel-related data.

**DATABASE INITIAL STATE:**
```sql
-- Check vessels
SELECT COUNT(*) FROM master_data_entries WHERE data_master_id = '001';
-- Expected: 6 vessels from migration

SELECT * FROM master_data_entries 
WHERE data_master_id = '001' 
ORDER BY code;
-- Review vessel data (code, description, status)

-- Check vessel groups
SELECT COUNT(*) FROM vessel_groups;
-- Expected: 2 vessel groups

SELECT * FROM vessel_groups;
-- Review vessel groups

-- Check vessel drafts, revisions, ranks
SELECT COUNT(*) FROM vessel_drafts;
SELECT COUNT(*) FROM vessel_revisions;
SELECT COUNT(*) FROM vessel_ranks;
```

**1. VESSELS (Master Data Entries) - CRUD OPERATIONS**

   A. CREATE - Add New Vessel
   ---------------------------
   Test: POST /api/master-data-entries
   
   Payload:
```json
   {
     "dataMasterId": "001",
     "code": "VSL-TEST-001",
     "description": "MV Test Vessel",
     "status": "Active",
     "metadata": {
       "vesselType": "Oil Tanker",
       "flag": "Panama",
       "imo": "IMO1234567",
       "dwt": 50000,
       "yearBuilt": 2020
     }
   }
```
   
   Expected:
   - [ ] Returns 201 Created
   - [ ] Auto-generated ID (sequence-based)
   - [ ] Data appears in database
   
   SQL Verify:
```sql
   SELECT * FROM master_data_entries 
   WHERE code = 'VSL-TEST-001';
```

   B. READ - Get All Vessels
   -------------------------
   Test: GET /api/master-data-entries?dataMasterId=001
   
   Expected:
   - [ ] Returns 200 OK
   - [ ] Returns 7 vessels (6 + 1 new)
   - [ ] Filtered by data_master_id = '001'
   
   C. READ - Get Single Vessel
   ----------------------------
   Test: GET /api/master-data-entries/:id
   
   Expected:
   - [ ] Returns 200 OK
   - [ ] Returns complete vessel data
   
   D. UPDATE - Modify Vessel
   -------------------------
   Test: PUT /api/master-data-entries/:id
   
   Payload:
```json
   {
     "description": "MV Updated Test Vessel",
     "status": "Inactive"
   }
```
   
   Expected:
   - [ ] Returns 200 OK
   - [ ] Changes persist in database
   
   E. DELETE - Remove Vessel
   -------------------------
   Test: DELETE /api/master-data-entries/:id
   
   Expected:
   - [ ] Returns 200 OK
   - [ ] Vessel removed from database

**2. VESSEL GROUPS - CRUD OPERATIONS**

   A. CREATE - Add Vessel Group
   -----------------------------
   Test: POST /api/vessel-groups
   
   Payload:
```json
   {
     "name": "Test Fleet",
     "description": "Test vessel group for testing",
     "vessels": ["VSL-001", "VSL-002"]
   }
```
   
   Expected:
   - [ ] Returns 201 Created
   - [ ] Auto-generated ID
   
   B. READ - Get All Vessel Groups
   -------------------------------
   Test: GET /api/vessel-groups
   
   Expected:
   - [ ] Returns 200 OK
   - [ ] Returns 3 groups (2 + 1 new)
   
   C. READ - Get Single Vessel Group
   ---------------------------------
   Test: GET /api/vessel-groups/:id
   
   Expected:
   - [ ] Returns 200 OK
   - [ ] Returns complete group data
   
   D. UPDATE - Modify Vessel Group
   -------------------------------
   Test: PUT /api/vessel-groups/:id
   
   Payload:
```json
   {
     "name": "Updated Fleet Name",
     "vessels": ["VSL-001", "VSL-002", "VSL-003"]
   }
```
   
   Expected:
   - [ ] Returns 200 OK
   - [ ] Changes persist
   
   E. DELETE - Remove Vessel Group
   -------------------------------
   Test: DELETE /api/vessel-groups/:id
   
   Expected:
   - [ ] Returns 200 OK
   - [ ] Group removed

**3. VESSEL DRAFTS - CRUD OPERATIONS**

   A. CREATE - Add Vessel Draft
   -----------------------------
   Test: POST /api/vessel-drafts
   
   Payload:
```json
   {
     "vesselId": "VSL-001",
     "draftType": "Summer",
     "forwardDraft": 10.5,
     "aftDraft": 11.2,
     "meanDraft": 10.85,
     "recordedDate": "2025-01-13"
   }
```
   
   Expected:
   - [ ] Returns 201 Created
   - [ ] Auto-generated ID
   
   B. READ - Get Drafts by Vessel
   ------------------------------
   Test: GET /api/vessel-drafts?vesselId=VSL-001
   
   Expected:
   - [ ] Returns drafts for specific vessel
   - [ ] Filtering works

**4. VESSEL REVISIONS - CRUD OPERATIONS**

   A. CREATE - Add Vessel Revision
   --------------------------------
   Test: POST /api/vessel-revisions
   
   Payload:
```json
   {
     "vesselId": "VSL-001",
     "revisionDate": "2025-01-13",
     "revisionType": "Major Upgrade",
     "description": "Engine overhaul completed",
     "performedBy": "ABC Shipyard"
   }
```
   
   Expected:
   - [ ] Returns 201 Created
   
   B. READ - Get Revisions by Vessel
   ---------------------------------
   Test: GET /api/vessel-revisions?vesselId=VSL-001
   
   Expected:
   - [ ] Returns revisions for specific vessel

**5. VESSEL RANKS - CRUD OPERATIONS**

   Test: POST /api/vessel-ranks
   
   Payload:
```json
   {
     "vesselId": "VSL-001",
     "rankId": "R001",
     "crewCount": 25,
     "positions": ["Master", "Chief Officer", "2nd Officer"]
   }
```
   
   Expected:
   - [ ] Creates vessel rank configuration

**6. PERSISTENCE TEST**

   - [ ] Create vessel, vessel group, draft, revision
   - [ ] Restart server
   - [ ] Verify all data persists

**7. VALIDATION TESTS**

   Test invalid vessel data:
```json
   {
     "dataMasterId": "",
     "code": "",
     "description": ""
   }
```
   
   Expected:
   - [ ] Returns 400 Bad Request
   - [ ] Validation errors returned

**8. EDGE CASES**

   A. Invalid IDs
   --------------
   - [ ] GET /api/vessel-groups/99999 → 404
   - [ ] GET /api/vessel-drafts/99999 → 404
   
   B. Duplicate Vessel Code
   ------------------------
   Try creating vessel with existing code
   
   Expected:
   - [ ] Returns 400 or 409 Conflict

**9. PERFORMANCE**

   - [ ] GET all vessels: < 1 second
   - [ ] GET vessel groups: < 500ms
   - [ ] Single vessel lookup: < 300ms

TESTING PROCEDURE:
1. Check initial database state
2. Test vessel CRUD (master_data_entries)
3. Test vessel groups CRUD
4. Test vessel drafts CRUD
5. Test vessel revisions CRUD
6. Test vessel ranks CRUD
7. Restart and verify persistence
8. Test validation and edge cases
9. Check performance

REPORT FORMAT:
MODULE 5: VESSELS
Initial State: Vessels: 6 ✅ Vessel Groups: 2 ✅ Vessel Drafts: 0 ✅ Vessel Revisions: 0 ✅
Vessels (Master Data Entries): CREATE ✅/❌: POST /api/master-data-entries → 201 READ ✅/❌: GET /api/master-data-entries → 200 (7 vessels) UPDATE ✅/❌: PUT /api/master-data-entries/:id → 200 DELETE ✅/❌: DELETE /api/master-data-entries/:id → 200
Vessel Groups: CREATE ✅/❌: Works READ ✅/❌: Works UPDATE ✅/❌: Works DELETE ✅/❌: Works
Vessel Drafts: CREATE ✅/❌: Works READ ✅/❌: Works with filtering
Vessel Revisions: CREATE ✅/❌: Works READ ✅/❌: Works with filtering
PERSISTENCE ✅/❌: All data persists
PERFORMANCE: List vessels: Xms ✅ Vessel groups: Xms ✅
ISSUES FOUND: [List any issues]
STATUS: ✅ WORKING / ⚠️ ISSUES / ❌ BROKEN

CRITICAL CHECKS:
- Vessel code uniqueness
- Vessel group vessel references (valid vessels?)
- Foreign key relationships
- Sequence reset working (from Module 1 fix)
- JSON metadata handling

After completing Module 5, report results and wait for approval before Module 6.

