Replit Prompt: Add Additional Groups Master External Data
Update the AdminModule component in client/src/pages/Admin/AdminModule.tsx to use external additional groups data instead of internal master data for Master ID "017". Follow these steps precisely:
________________________________________
Step 1: Add Import Statement
At the top of the file, after the existing hook imports, add:
typescript
import { useExternalAdditionalGroups } from "@/hooks/useExternalAdditionalGroups";
________________________________________
Step 2: Add Additional Groups Data Fetching
Find the section where other external hooks are called (near nationality, vessel types, vessels, and fleet groups hooks).
ADD this new code:
typescript
// NEW: External additional groups data from API (Master 017)
const {
  data: externalAdditionalGroupsData,
  isLoading: additionalGroupsLoading,
  error: additionalGroupsError,
} = useExternalAdditionalGroups();

// Process response - some APIs return an object with `additionalGroups`, others return array directly
const additionalGroupsData = (externalAdditionalGroupsData as any)?.additionalGroups || externalAdditionalGroupsData || [];

// Add debug logging (development only)
if (import.meta.env.DEV) {
  console.log('🧩 [External Additional Groups] Processed Data:', additionalGroupsData);
}
________________________________________
Step 3: Update Additional Groups Rendering in renderDataMastersModule
Find the renderDataMastersModule function (around line 2800). Locate where other external master conditions are rendered (after nationality "001", vessel types "004", vessels "014", fleet groups "015", etc.).
ADD this code block in the appropriate position in the conditional chain:
typescript
) : selectedMaster === "017" ? (
  // Special handling for Additional Groups Master (017) - Use external API data
  additionalGroupsLoading ? (
    <div className="p-3 text-xs text-gray-500">Loading additional groups...</div>
  ) : additionalGroupsError ? (
    <div className="p-3 text-xs text-red-500">Error loading additional groups: {additionalGroupsError.message}</div>
  ) : additionalGroupsData && additionalGroupsData.length > 0 ? (
    additionalGroupsData.map((item: any, index: number) => (
      <div
        key={item.id || `additional-group-${index}`}
        className="grid grid-cols-4 gap-0 border-b border-gray-100 hover:bg-gray-50"
      >
        {/* Column 1: Entry ID */}
        <div className="p-3 border-r border-gray-200">
          <span className="text-xs text-gray-700">
            { item.id || <em className="text-gray-400">No entry ID</em>}
          </span>
        </div>

        {/* Column 2: Group Name */}
        <div className="p-3 border-r border-gray-200">
          <span className="text-xs text-gray-700">
            { item.name || <em className="text-gray-400">No group name</em>}
          </span>
        </div>

        {/* Column 3: Description/Type (if available) */}
        <div className="p-3 border-r border-gray-200">
          <span className="text-xs text-gray-700">
            {item.vessels || <em className="text-gray-400">No description</em>}
          </span>
        </div>

        {/* Column 4: Actions - External data (read-only) */}
        <div className="p-3 flex justify-center">
          <span className="text-xs text-gray-400">External</span>
        </div>
      </div>
    ))
  ) : (
    <div className="p-3 text-xs text-gray-500">No additional groups found</div>
  )
) : (
Note: The final ) : ( should flow into the next master condition or the default master data mapping logic.
________________________________________
Step 4: Verify Error Handling
Ensure additionalGroupsError is properly displayed with descriptive error messages as shown in the code block above.
________________________________________
Important Notes:
1.	✅ Keep ALL existing functionality for other master types completely unchanged
2.	✅ Only add rendering logic for master ID "017" (additional groups)
3.	✅ The external data is READ-ONLY - no edit/delete functionality
4.	✅ Maintain the same grid layout and styling as other external masters
5.	✅ Test that switching between masters still works correctly
6.	✅ Verify that the "External" label appears in the Actions column
7.	✅ Check that error states display properly when API calls fail
8.	✅ The data processor handles TWO possible API response formats: 
o	Wrapped object: { additionalGroups: [...] }
o	Direct array: [...]
9.	✅ Console logging only appears in development mode (not production)
________________________________________
Testing Checklist:
•	Additional Groups Master (017) loads and displays external data
•	Entry ID displays correctly (agid or id)
•	Group name displays correctly
•	Description/type displays correctly (or placeholder if not available)
•	Switching to other masters still works
•	Loading states show during data fetch
•	Error messages display when API fails
•	No edit/delete buttons appear for external data (only "External" label)
•	Console logs show processed data structure (in dev mode only)
•	Data persists correctly when navigating away and back
•	Empty states display when no data is available
•	Works with both wrapped and direct array API responses

