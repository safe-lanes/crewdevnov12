# Task: Remove Data from Vessel Module's "Crew O/B" Column

## Objective
Clear all data from the "Crew O/B" (Crew On Board) column in the vessel module while keeping the column structure intact.

## Steps to Execute

### Step 1: Identify the Column
First, let's find the exact column name in the vessels table:
```bash
# Show vessels table structure
psql "$DATABASE_URL" -c "\d vessels"

# Search for crew-related columns
psql "$DATABASE_URL" -c "SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'vessels' AND column_name LIKE '%crew%';"
```

### Step 2: Check Current Data
Before clearing, let's see what data exists:
```bash
# Show current crew o/b data
psql "$DATABASE_URL" -c "SELECT id, name, crew_onboard FROM vessels WHERE crew_onboard IS NOT NULL LIMIT 10;"
# (Adjust column name based on Step 1 results)
```

### Step 3: Create Migration File
Create a new migration file to clear the data:

**File:** `migrations/0037_clear_crew_onboard_data.sql`
```sql
-- Migration: Clear Crew O/B column data
-- Date: December 17, 2025
-- Purpose: Remove all data from crew onboard column while keeping column structure

-- Clear the crew onboard data
UPDATE vessels 
SET crew_onboard = NULL 
WHERE crew_onboard IS NOT NULL;

-- Verification query
SELECT COUNT(*) as rows_with_crew_data 
FROM vessels 
WHERE crew_onboard IS NOT NULL;

-- Expected result: 0
```

### Step 4: Apply the Migration
```bash
# Apply migration
psql "$DATABASE_URL" -f migrations/0037_clear_crew_onboard_data.sql

# Verify data is cleared
psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM vessels WHERE crew_onboard IS NOT NULL;"
```

### Step 5: Update Migration Tracking
```bash
# Add to migration tracking
psql "$DATABASE_URL" -c "INSERT INTO schema_migrations (name, executed_at) VALUES ('0037_clear_crew_onboard_data', NOW());"
```

## Questions to Confirm

Before executing, please confirm:

1. **Column Name:** What is the exact column name?
   - crew_onboard?
   - crew_ob?
   - crew_on_board?
   - Something else?

2. **Scope:** Should we clear:
   - All vessels? OR
   - Only specific vessels?

3. **Backup:** Should I create a backup before clearing?

## If Column Name is Different

If the column name is not `crew_onboard`, please tell me the exact name and I'll adjust the migration accordingly.

## Output Required

Please execute Steps 1-2 and provide:
- Exact column name for "Crew O/B"
- Current row count with data
- Confirmation to proceed with clearing

Then I'll create and apply the correct migration.