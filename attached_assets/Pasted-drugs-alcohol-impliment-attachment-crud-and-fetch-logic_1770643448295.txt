drugs & alcohol

impliment attachment crud and fetch logic with proper join

Let me examine the current attachment setup across V1 and V2 to understand what needs to be built.

Now let me look at the V1 attachment implementation and the V2 frontend form to understand how attachments work:

Now let me check the V1 attachment handling in the frontend and the test records repository to understand the full picture:

Now let me look at the V1 attachment implementation and the FileAttachmentDialog component:

Now let me see how the form saves the data and how the V2 API client handles attachments:

Now let me check how the parent module calls onSave and passes attachment data to the API:

Now let me see how handleSaveForm processes attachments:

Now I have a full picture. The issue is clear: the form tracks attachments in state, but the transformFormDataForAPI maps data.attachmentFile (which is never set from the form). The form's attachments array never gets serialized and sent to the API. Let me also check how existing attachments are loaded back:

Now I have the complete picture. Here's the problem:

The form uses attachments field (array of objects with {id, name, type, size, data, uploadedAt})
The transformFormDataForAPI maps data.attachmentFile — but the form never sets attachmentFile, it uses attachments
When loading existing records, existingRecord.attachments doesn't exist — the response has attachmentFile (JSON string from transformToV1Response)
The attachment data needs to flow: form attachments → API attachmentFile → backend saves to da_attachments_v2 → API response attachmentFile → form parses back to attachments
Now let me fix both the save flow and the load flow. The form has attachments but the API expects attachmentFile as JSON string.

Now fix the loading side. The API response returns attachmentFile (JSON string), but the form loads existingRecord.attachments. Let me update the form to properly parse attachmentFile when loading:

Good. Now let me mark task 1 & 2 as done and move to adding the standalone attachment CRUD endpoints:

Now let me add standalone attachment endpoints. I need an attachment controller with individual upload, get-by-test-record, and delete operations. Let me also update the repository with a softDeleteByUuid method:

Now create the attachment controller:

Now update the controller index and routes:

Now add the API client methods for standalone attachment operations:

Now let me wire up the onDeleteAttachment callback in the V2 form. Let me look at the form's attachment dialog usage:

Now I need to add the onDeleteAttachment callback and import the API. Let me check what imports are already at the top of the form:

Now let me add the onDeleteAttachment handler to the form. The FileAttachmentDialog already supports an onDeleteAttachment prop:

Now let me also make sure the delete route for attachments doesn't conflict with the test records delete route. Looking at the routes, DELETE /attachments/:attUuid is distinct from DELETE /test-records/:uuid, so we're fine. Let me now restart and test:

Server is running cleanly. Let me now test the full flow — create a test record with an attachment:

Need to use a different date. Let me clean that up and try again:

There are already V1 records for this vessel. Let me check what exists and use a unique date:

Those are V1 records. Let me use a different date:

The record was created with the attachment properly saved. The attachmentFile in the response shows the attachment with its UUID. Now let me test the standalone attachment endpoints:

The standalone fetch endpoint works. Now let me test the delete endpoint:

The standalone upload worked. Now let's test the delete:

All CRUD operations work correctly. Now let me clean up the test record and update task statuses:

All tests pass. Let me now run the architect review for the complete implementation:

The attachment system is now fully implemented for the V2 Drugs & Alcohol module. Here's what was done:

Data Flow Fixed:

When saving a form, the attachments array from the form is now properly serialized to attachmentFile JSON for the API
When loading an existing record, the form correctly parses back the attachmentFile field from the API response
Standalone Attachment Endpoints Added:

GET /api/v2/drugs-alcohol/attachments/:testRecordUuid — fetch all attachments for a test record
POST /api/v2/drugs-alcohol/attachments/:testRecordUuid — upload a new attachment to a test record
DELETE /api/v2/drugs-alcohol/attachments/:attUuid — soft-delete a specific attachment
Form Integration:

The Attachment(s) button in the form now properly saves and loads files
Individual attachment deletion works through the dialog's delete button, calling the V2 API directly
All operations were verified end-to-end: creating records with attachments, fetching them back, uploading standalone attachments, and deleting individual attachments.

