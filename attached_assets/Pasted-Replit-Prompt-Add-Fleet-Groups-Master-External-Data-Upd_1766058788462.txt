Replit Prompt: Add Fleet Groups Master External Data
Update the AdminModule component in client/src/pages/Admin/AdminModule.tsx to use external fleet groups data instead of internal master data for Master ID "015". Follow these steps precisely:
________________________________________
Step 1: Add Import Statement
At the top of the file, after the existing hook imports, add:
typescript
import { useExternalFleetGroups } from "@/hooks/useExternalFleetGroups";
________________________________________
Step 2: Add Fleet Groups Data Fetching
Find the section where other external hooks are called (near nationality, vessel types, and vessels hooks).
ADD this new code:
typescript
// NEW: External fleet groups data from API
const { 
  data: externalFleetGroupsData, 
  isLoading: fleetGroupsLoading, 
  error: fleetGroupsError 
} = useExternalFleetGroups();

// Process external API response structure
const fleetGroups = (externalFleetGroupsData as any)?.fleetGroups || [];
________________________________________
Step 3: Update Fleet Groups Rendering in renderDataMastersModule
Find the renderDataMastersModule function (around line 2800). Locate where other external master conditions are rendered (after nationality "001", vessel types "004", vessels "014", etc.).
ADD this code block in the appropriate position in the conditional chain:
typescript
) : selectedMaster === "015" ? (
  // Special handling for Fleet Groups Master (015) - Use external API data
  fleetGroupsLoading ? (
    <div className="p-3 text-xs text-gray-500">Loading fleet groups...</div>
  ) : fleetGroupsError ? (
    <div className="p-3 text-xs text-red-500">Error loading fleet groups: {fleetGroupsError.message}</div>
  ) : fleetGroups && fleetGroups.length > 0 ? (
    fleetGroups.map((item: any, index: number) => (
      <div
        key={item.id || `fleet-group-${index}`}
        className="grid grid-cols-4 gap-0 border-b border-gray-100 hover:bg-gray-50"
      >
        {/* Column 1: Entry ID */}
        <div className="p-3 border-r border-gray-200">
          <span className="text-xs text-gray-700">
            {item.id || <em className="text-gray-400">No entry ID</em>}
          </span>
        </div>

        {/* Column 2: Fleet Group Name */}
        <div className="p-3 border-r border-gray-200">
          <span className="text-xs text-gray-700">
            {item.name || <em className="text-gray-400">No name</em>}
          </span>
        </div>

        {/* Column 3: Vessels */}
        <div className="p-3 border-r border-gray-200">
          <span className="text-xs text-gray-700">
            {item.vessels || <em className="text-gray-400">No vessels</em>}
          </span>
        </div>

        {/* Column 4: Actions - External data (read-only) */}
        <div className="p-3 flex justify-center">
          <span className="text-xs text-gray-400">External</span>
        </div>
      </div>
    ))
  ) : (
    <div className="p-3 text-xs text-gray-500">No fleet groups found</div>
  )
) : (
Note: The final ) : ( should flow into the next master condition or the default master data mapping logic.
________________________________________
Step 4: Verify Error Handling
Ensure fleetGroupsError is properly displayed with descriptive error messages as shown in the code block above.
________________________________________
Important Notes:
1.	✅ Keep ALL existing functionality for other master types completely unchanged
2.	✅ Only add rendering logic for master ID "015" (fleet groups)
3.	✅ The external data is READ-ONLY - no edit/delete functionality
4.	✅ Maintain the same grid layout and styling as other external masters
5.	✅ Test that switching between masters still works correctly
6.	✅ Verify that the "External" label appears in the Actions column
7.	✅ Check that error states display properly when API calls fail
8.	✅ Ensure the data structure matches your API response: { fleetGroups: [...] }
________________________________________
Testing Checklist:
•	Fleet Groups Master (015) loads and displays external data
•	Entry ID displays correctly
•	Fleet Group name displays correctly
•	Vessels list displays correctly (comma-separated string)
•	Switching to other masters still works
•	Loading states show during data fetch
•	Error messages display when API fails
•	No edit/delete buttons appear for external data (only "External" label)
•	Console logs show processed data structure
•	Data persists correctly when navigating away and back
•	Empty states display when no data is available

