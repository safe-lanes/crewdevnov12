Excellent work on Module 3! All issues fixed proactively. ✅

Proceed to MODULE 4: CREW POOL (Crew Management) Testing

This is the LARGEST and most critical module - the heart of your system. Test thoroughly!

CREW POOL MODULE TESTING
=========================

Crew management handles all crew member operations, profiles, and assignments.

**DATABASE INITIAL STATE:**
```sql
SELECT COUNT(*) FROM crew_members;
-- Expected: 80 crew members from migration

SELECT id, first_name, last_name, present_rank, present_vessel 
FROM crew_members LIMIT 5;
-- Review sample crew data
```

**1. CREW MEMBERS - BASIC CRUD OPERATIONS**

   A. CREATE - Add New Crew Member
   --------------------------------
   Test: POST /api/crew-members
   
   Payload:
```json
   {
     "firstName": "John",
     "middleName": "Test",
     "lastName": "Doe",
     "dob": "1990-05-15",
     "nationality": "Filipino",
     "presentRank": "Third Officer",
     "presentVessel": "MV Test Ship",
     "joiningDate": "2025-01-01",
     "contractEndDate": "2025-12-31",
     "email": "john.doe@example.com",
     "phone": "+1234567890",
     "passportNumber": "P1234567",
     "seamanBookNumber": "SB123456",
     "status": "Active"
   }
```
   
   Expected:
   - [ ] Returns 201 Created
   - [ ] Returns crew object with auto-generated ID (e.g., "A0081")
   - [ ] Uses getNextCrewId() for ID generation
   - [ ] Data appears in database
   
   SQL Verify:
```sql
   SELECT * FROM crew_members 
   WHERE email = 'john.doe@example.com';
```

   B. READ - Get All Crew Members
   ------------------------------
   Test: GET /api/crew-members
   
   Expected:
   - [ ] Returns array of 81 crew members (80 + 1 new)
   - [ ] Returns 200 OK
   - [ ] All required fields present
   - [ ] Performance: < 2 seconds for 81 records

   C. READ - Get Single Crew Member
   ---------------------------------
   Test: GET /api/crew-members/:id
   
   Use ID from CREATE test (e.g., "A0081")
   
   Expected:
   - [ ] Returns 200 OK
   - [ ] Returns complete crew object
   - [ ] All fields accurate

   D. UPDATE - Modify Crew Member
   ------------------------------
   Test: PUT /api/crew-members/:id
   
   Payload:
```json
   {
     "presentRank": "Second Officer",
     "presentVessel": "MV Updated Ship",
     "status": "On Leave"
   }
```
   
   Expected:
   - [ ] Returns 200 OK
   - [ ] Returns updated crew object
   - [ ] Changes persist in database
   
   SQL Verify:
```sql
   SELECT present_rank, present_vessel, status 
   FROM crew_members WHERE id = 'A0081';
   -- Should show: Second Officer, MV Updated Ship, On Leave
```

   E. DELETE - Remove Crew Member
   ------------------------------
   Test: DELETE /api/crew-members/:id
   
   Expected:
   - [ ] Returns 200 OK or 204 No Content
   - [ ] Crew member removed from database
   - [ ] OR soft-deleted (status = 'Deleted')
   
   SQL Verify:
```sql
   SELECT COUNT(*) FROM crew_members WHERE id = 'A0081';
   -- Should return 0 (or status = 'Deleted')
```

**2. CREW FILTERING & SEARCH**

   A. Filter by Rank
   -----------------
   Test: GET /api/crew-members?rank=Master
   
   Expected:
   - [ ] Returns only crew with rank "Master"
   - [ ] Filtering works correctly
   
   B. Filter by Vessel
   -------------------
   Test: GET /api/crew-members?vessel=MV%20Test%20Ship
   
   Expected:
   - [ ] Returns only crew on specified vessel
   
   C. Filter by Status
   -------------------
   Test: GET /api/crew-members?status=Active
   
   Expected:
   - [ ] Returns only active crew members
   
   D. Search by Name
   -----------------
   Test: GET /api/crew-members?search=John
   
   Expected:
   - [ ] Returns crew members with "John" in name
   - [ ] Case-insensitive search works

**3. CREW DOCUMENTS & CERTIFICATIONS**

   If crew members have related documents/certificates:
   
   A. Get Crew Documents
   ---------------------
   Test: GET /api/crew-members/:id/documents
   
   Expected:
   - [ ] Returns list of documents
   - [ ] Or empty array if none
   
   B. Add Document
   ---------------
   Test: POST /api/crew-members/:id/documents
   
   Payload:
```json
   {
     "documentType": "Passport",
     "documentNumber": "P1234567",
     "issueDate": "2020-01-01",
     "expiryDate": "2030-01-01",
     "issuingCountry": "Philippines"
   }
```
   
   Expected:
   - [ ] Document added successfully
   - [ ] Returns 201 Created

**4. CREW HISTORY & ASSIGNMENTS**

   A. Get Crew Service History
   ----------------------------
   Test: GET /api/crew-members/:id/history
   
   Expected:
   - [ ] Returns employment history
   - [ ] Shows previous vessels, ranks, dates
   
   B. Get Current Assignment
   -------------------------
   Test: GET /api/crew-members/:id/current-assignment
   
   Expected:
   - [ ] Returns current vessel assignment
   - [ ] Or null if not assigned

**5. BULK OPERATIONS**

   A. Bulk Status Update
   ---------------------
   Test: POST /api/crew-members/bulk-update-status
   
   Payload:
```json
   {
     "crewIds": ["A0001", "A0002"],
     "status": "On Leave"
   }
```
   
   Expected:
   - [ ] Updates multiple crew members
   - [ ] Returns success count

**6. DATA VALIDATION**

   Test invalid crew data:
```json
   {
     "firstName": "",
     "dob": "invalid-date",
     "email": "not-an-email"
   }
```
   
   Expected:
   - [ ] Returns 400 Bad Request
   - [ ] Returns detailed validation errors
   - [ ] No database record created

**7. PERSISTENCE TEST**

   - [ ] Create crew member A0081
   - [ ] Restart server
   - [ ] GET crew member A0081
   - [ ] Verify all data intact

**8. EDGE CASES**

   A. Invalid Crew ID
   ------------------
   Test: GET /api/crew-members/INVALID-ID-999
   
   Expected:
   - [ ] Returns 404 Not Found
   
   B. Duplicate Email/Phone
   -------------------------
   Try creating crew with existing email
   
   Expected:
   - [ ] Should handle gracefully (allow OR return 400)
   
   C. Missing Required Fields
   ---------------------------
   Test: POST /api/crew-members with minimal data
   
   Expected:
   - [ ] Returns 400 with validation errors

**9. PERFORMANCE TESTS**

   - [ ] GET /api/crew-members with 80+ records: < 2 seconds
   - [ ] Search operations: < 1 second
   - [ ] Single crew lookup: < 500ms

TESTING PROCEDURE:
1. Check initial database state (80 crew members)
2. Test CREATE with full crew data
3. Test all READ operations (list, single, filters)
4. Test UPDATE operations
5. Test DELETE operation
6. Test search and filtering
7. Test related operations (documents, history, assignments)
8. Restart server and verify persistence
9. Test edge cases and validation
10. Verify performance is acceptable

REPORT FORMAT:
MODULE 4: CREW POOL (CREW MANAGEMENT)
Initial State: SELECT COUNT(*) FROM crew_members; -- 80 ✅
CREATE ✅/❌: POST /api/crew-members → 201 Created New Crew ID: A0081 (auto-generated) ✅ SQL Verification: 1 row found ✅
READ Operations: GET /api/crew-members → 200 OK (81 crew) ✅ GET /api/crew-members/A0081 → 200 OK ✅ GET /api/crew-members?rank=Master → 200 OK (filtered) ✅ GET /api/crew-members?search=John → 200 OK (search works) ✅
UPDATE ✅/❌: PUT /api/crew-members/A0081 → 200 OK Rank updated: Third Officer → Second Officer ✅
DELETE ✅/❌: DELETE /api/crew-members/A0081 → 200 OK SQL Verification: 0 rows (deleted) ✅
FILTERING ✅/❌: By Rank: Works ✅ By Vessel: Works ✅ By Status: Works ✅ Search: Works ✅
PERSISTENCE ✅/❌: Created A0082 → Restarted → Still exists ✅
PERFORMANCE: List 81 crew: 1.2 seconds ✅ Search: 0.4 seconds ✅ Single lookup: 0.15 seconds ✅
ISSUES FOUND:
1.	[List any issues]
STATUS: ✅ WORKING / ⚠️ ISSUES / ❌ BROKEN
CRITICAL AREAS TO CHECK:
- ID auto-generation (getNextCrewId)
- Validation on required fields
- Email/phone uniqueness (if enforced)
- Data type validation (dates, numbers)
- Filtering and search performance
- Relationship with other tables (appraisals, planning, etc.)

This is the MOST IMPORTANT module - test thoroughly!

After completing Module 4, report results and wait for approval before Module 5.
