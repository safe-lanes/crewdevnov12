You are working on my existing React + TypeScript project hosted here in Replit.

Goal
Refactor the project to a module-first structure with shared, reusable components and clear separation of concerns. Apply the 8 requirements below without changing app behavior.

Key Requirements
1) Move all shared/common components into client/src/components/** with clear subfolders:
   - components/common     -> pure UI pieces (buttons, modals, tables, etc.)
   - components/layout     -> Header, Sidebar, Page, Breadcrumbs
   - components/feedback   -> ErrorBoundary, Loader/Spinner, EmptyState, Toast hooks
   - components/form       -> RHF-aware inputs: TextField, SelectField, DateField, etc.
   Keep any shadcn/ui set under components/ui as-is, but convert page-specific UI into reusable components above.

2) Place all static/mock data under client/src/utils/data/** (e.g., ranks.ts, sampleLists.ts).

3) Enforce naming conventions:
   - Folders: lowercase (modules, components, utils, hooks, lib, styles, types)
   - Component files: PascalCase.tsx (CrewForm.tsx)
   - Hooks: useThing.ts
   - Zod schemas: *.schema.ts
   - Services (API): *.api.ts
   - Types: *.types.ts
   - Each module has index.ts that re-exports its public parts.

4) Ensure components are reusable:
   - No API calls in presentational components.
   - Accept props for data/callbacks and className; use forwardRef where helpful.
   - Extract duplicated JSX from pages into components/common or module-level components.

5) Implement error handling & validation:
   - Create components/feedback/AppErrorBoundary.tsx and wrap App root with it.
   - Add a normalized fetch helper at utils/http.ts that throws on !res.ok with a meaningful Error message.
   - Use zod + react-hook-form for forms; create module-level schemas in modules/**/validation/*.schema.ts.

6) Use async/await for all async operations; remove .then chains where feasible.

7) Documentation & comments:
   - Add README.md inside each module describing its pages, components, services, and data flow.
   - Use JSDoc for public functions in services and hooks.

8) Module (menu) structure:
   - Create client/src/modules/<module>/ with:
     pages/, components/, services/, validation/, hooks/, types/, index.ts
   - Implement an example module "crewing":
     pages/CrewingListPage.tsx, CrewingAddPage.tsx, CrewingEditPage.tsx
     components/CrewForm.tsx, CrewTable.tsx
     services/crew.api.ts
     validation/crew.schema.ts
     types/crew.types.ts
     hooks/useCrew.ts
   - If existing pages or logic already cover these screens, move them into this structure. Otherwise scaffold minimal implementations that compile.

Project Layout Target
client/src/
  app/
    App.tsx
    routes.tsx
    main.tsx
  components/
    common/
    layout/
    feedback/
      AppErrorBoundary.tsx
    form/
    ui/            (keep existing shadcn components)
  modules/
    crewing/
      pages/
        CrewingListPage.tsx
        CrewingAddPage.tsx
        CrewingEditPage.tsx
      components/
        CrewForm.tsx
        CrewTable.tsx
      services/
        crew.api.ts
      validation/
        crew.schema.ts
      hooks/
        useCrew.ts
      types/
        crew.types.ts
      index.ts
    (repeat for other modules as needed)
  utils/
    data/
      ranks.ts
    http.ts
    format.ts
    validation.ts
  hooks/
    use-toast.ts   (or equivalent global hooks)
  lib/
    queryClient.ts
  styles/
    index.css
  types/
    index.d.ts

Routing
- Ensure routes.tsx imports module pages from "@/modules/<name>/pages/*".
- Keep router behavior unchanged.

Paths/Aliases
- If not present, configure tsconfig.json to allow "@/..." imports:
  "compilerOptions": {
    "baseUrl": "client/src",
    "paths": { "@/*": ["*"] }
  }
- Update imports accordingly.

HTTP Helper (utils/http.ts)
- Create an async api<T>(input, init) wrapper using fetch and JSON handling.
- Attach default headers and convert non-2xx into thrown Error with message from server if available.

Validation Example (modules/crewing/validation/crew.schema.ts)
- Define zod object with basic constraints (name, rank, etc.) and export type with z.infer.

Error Boundary
- components/feedback/AppErrorBoundary.tsx: standard React error boundary that renders a friendly message and optional retry slot.

Reusability
- Convert any page-level inline form inputs into components/form/* controlled by RHF Controller.
- Ensure all presentational components accept data via props and do not perform side effects.

Refactor Steps
1) Detect the current source root (likely client/src). Confirm before moving.
2) Create new folders per the layout above.
3) Move files from pages -> modules/*/pages and extract shared parts into components/common|form|layout|feedback.
4) Create utils/http.ts and migrate API calls in pages/components into module services/*.api.ts.
5) Replace .then chains with async/await.
6) Introduce zod schemas under validation and wire into forms with zodResolver.
7) Add AppErrorBoundary and wrap App.
8) Add utils/data for static constants; replace inline constants.
9) Add index.ts in each module to re-export components/pages/services as needed.
10) Update imports to "@/..." paths.
11) Run TypeScript check and build to confirm.
12) Output a complete summary of changes including:
    - File moves/creations/deletions
    - Updated imports count
    - Any TODOs left with reasons

Deliverables in this chat
- A short plan, then the patched file tree.
- Diffs or full file contents for any new/modified files (limit to relevant parts if large).
- Instructions for running (dev/build) after refactor.
- A checklist mapping the 8 requirements to the changes you made.

If any ambiguity exists (e.g., actual module names beyond "crewing"), ask me before proceeding.
