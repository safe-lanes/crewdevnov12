Replit Prompt: Add Nationality Master External Data
Update the AdminModule component in client/src/pages/Admin/AdminModule.tsx to use external nationality data instead of internal master data for Master ID "001". Follow these steps precisely:
________________________________________
Step 1: Add Import Statement
At the top of the file, after the existing hook imports, add:
typescript
import { useExternalNationalities } from "@/hooks/useNationalities";
________________________________________
Step 2: Replace Nationality Data Fetching
Find the nationality data fetching section (usually near the top of the component, after state declarations).
COMMENT OUT the existing code (if present):
typescript
// const { data: nationalityData = [], isLoading: nationalityLoading } = useMasterDataEntries('001', { 
//   enabled: selectedAdminPage === "masters" 
// });
ADD this new code immediately after:
typescript
// NEW: External nationality data from API
let {
  data: nationalityData = [],
  isLoading: nationalityLoading,
  error: nationalityError
} = useExternalNationalities();

// Process external API response structure
nationalityData = nationalityData.nationalities || [];
________________________________________
Step 3: Update Nationality Rendering in renderDataMastersModule
Find the renderDataMastersModule function (around line 2800). Locate the master data error handling section:
typescript
<div className="p-3 text-xs text-red-500">Error loading master data</div>
Immediately AFTER the closing ) : of the error condition, ADD this code block as the FIRST condition before any other selectedMaster checks:
typescript
) : selectedMaster === "001" ? (
  // Special handling for Nationalities Master (001) - Use external API data
  nationalityLoading ? (
    <div className="p-3 text-xs text-gray-500">Loading nationalities...</div>
  ) : nationalityError ? (
    <div className="p-3 text-xs text-red-500">Error loading nationalities: {nationalityError.message}</div>
  ) : nationalityData && nationalityData.length > 0 ? (
    nationalityData.map((item: any, index: number) => (
      <div
        key={item.id || item.country || `nationality-${index}`}
        className="grid grid-cols-4 gap-0 border-b border-gray-100 hover:bg-gray-50"
      >
        {/* Column 1: Entry ID */}
        <div className="p-3 border-r border-gray-200">
          <span className="text-xs text-gray-700">
            {item.cid || <em className="text-gray-400">No entry ID</em>}
          </span>
        </div>

        {/* Column 2: Nationality Name */}
        <div className="p-3 border-r border-gray-200">
          <span className="text-xs text-gray-700">
            {item.nationality || <em className="text-gray-400">No nationality</em>}
          </span>
        </div>

        {/* Column 3: Country Name */}
        <div className="p-3 border-r border-gray-200">
          <span className="text-xs text-gray-700">
            {item.countryName || <em className="text-gray-400">No country name</em>}
          </span>
        </div>

        {/* Column 4: Actions - External data (read-only) */}
        <div className="p-3 flex justify-center">
          <span className="text-xs text-gray-400">External</span>
        </div>
      </div>
    ))
  ) : (
    <div className="p-3 text-xs text-gray-500">No nationalities found</div>
  )
) : selectedMaster === "004" ? (
Note: The final line should flow into your next master condition (vessel types "004" or whichever master comes next in your code).
________________________________________
Step 4: Verify Error Handling
Ensure nationalityError is properly displayed with descriptive error messages as shown in the code block above.
________________________________________
Important Notes:
1.	✅ Keep ALL existing functionality for other master types (002, 003, 004, 012, 013, 014, 018, etc.) completely unchanged
2.	✅ Only modify the rendering logic for master ID "001" (nationalities)
3.	✅ The external data is READ-ONLY - no edit/delete functionality
4.	✅ Maintain the same grid layout and styling as other masters
5.	✅ Test that switching between masters still works correctly
6.	✅ Verify that the "External" label appears in the Actions column
7.	✅ Check that error states display properly when API calls fail
8.	✅ Ensure the data structure matches your API response: { nationalities: [...] }
________________________________________
Testing Checklist:
•	Nationality Master (001) loads and displays external data
•	Entry ID (cid) displays correctly
•	Nationality name displays correctly
•	Country name displays correctly
•	Switching to other masters still works
•	Loading states show during data fetch
•	Error messages display when API fails
•	No edit/delete buttons appear for external data (only "External" label)
•	Console logs show processed data structure
•	Data persists correctly when navigating away and back
•	Empty states display when no data is available

